import { CrawlerLink } from "./methodes/CrawlerLink.js";

export default function Routes(app) {
  // app.get("/test/", (req, res) => res.json({ isok: true }));
  // app.post("/outils/crawler-link/", (req, res) => CrawlerLink(req, res));
}

